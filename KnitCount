<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Knitting Row Counter</title>

  <style>
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: #f5f6f7;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }

    .card {
      background: white;
      padding: 24px;
      border-radius: 12px;
      width: 320px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.08);
    }

    h1 {
      margin-top: 0;
      text-align: center;
      font-size: 1.25rem;
    }

    .counter {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin: 16px 0;
    }

    .counter button {
      width: 44px;
      height: 44px;
      font-size: 1.25rem;
      font-weight: bold;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      background: #0d6efd;
      color: white;
    }

    .counter-value {
      font-size: 2rem;
      font-weight: bold;
      width: 80px;
      text-align: center;
    }

    label {
      font-size: 0.85rem;
      font-weight: 600;
      display: block;
      margin-bottom: 6px;
    }

    textarea {
      width: 100%;
      min-height: 100px;
      resize: vertical;
      padding: 8px;
      font-family: inherit;
      font-size: 0.9rem;
      border-radius: 6px;
      border: 1px solid #ccc;
    }
  </style>
</head>

<body>
  <div class="card">
    <h1>Knitting Row Counter</h1>

    <div class="counter">
      <button id="decrease">−</button>
      <div class="counter-value" id="count">0</div>
      <button id="increase">+</button>
    </div>

    <label for="notes">Instructions / Notes</label>
    <textarea id="notes" placeholder="Write your pattern instructions here..."></textarea>
  </div>

  <script>
    const countEl = document.getElementById("count");
    const notesEl = document.getElementById("notes");

    let count = Number(localStorage.getItem("rowCount")) || 0;
    let notes = localStorage.getItem("rowNotes") || "";

    countEl.textContent = count;
    notesEl.value = notes;

    document.getElementById("increase").addEventListener("click", () => {
      count++;
      countEl.textContent = count;
      localStorage.setItem("rowCount", count);
    });

    document.getElementById("decrease").addEventListener("click", () => {
      if (count > 0) {
        count--;
        countEl.textContent = count;
        localStorage.setItem("rowCount", count);
      }
    });

    notesEl.addEventListener("input", () => {
      localStorage.setItem("rowNotes", notesEl.value);
    });
  </script>
</body>
</html>
